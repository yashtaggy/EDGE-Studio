import { NextResponse } from 'next/server';

export async function POST() {
  return NextResponse.json({ message: 'AI integration pending; logic not implemented.' });
}

export async function GET() {
  return NextResponse.json({ message: 'AI integration pending; logic not implemented.' });
}
